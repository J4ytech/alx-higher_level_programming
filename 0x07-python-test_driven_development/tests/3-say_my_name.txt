Import the function:
    >>> say_my_name = __import__('3-say_my_name').say_my_name

Normal usage with both names:
    >>> say_my_name("John", "Smith")
    My name is John Smith

Usage with only the first name (last_name defaults to ""):
    >>> say_my_name("Walter")
    My name is Walter 

Testing for errors - first_name is not a string:
    >>> say_my_name(12, "White")
    Traceback (most recent call last):
        ...
    TypeError: first_name must be a string

Testing for errors - last_name is not a string:
    >>> say_my_name("Walter", 12)
    Traceback (most recent call last):
        ...
    TypeError: last_name must be a string

Testing with empty strings (technically still strings, so it should pass):
    >>> say_my_name("", "")
    My name is